using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Junk class for deletion, we are only gonna have guitars.
/// </summary>
public class KeyboardSynth : Instrument
{
    // Start is called before the first frame update
    new void Start()
    {
        base.Start();


    }

    // Update is called once per frame
    new void Update()
    {
        base.Update();

    }
}
